// Code generated by togo. Resource fragment — safe to edit.
package models

import "time"

// {{.Resource.Name}} maps to the "{{.Resource.Table}}" table.
type {{.Resource.Name}} struct {
	ID string `json:"id" db:"id"`
{{- range .Resource.Fields}}
	{{pascal .Name}} {{if .Null}}*{{end}}{{.Go}} `json:"{{.Name}}" db:"{{.Name}}"`
{{- end}}
	CreatedAt time.Time `json:"created_at" db:"created_at"`
	UpdatedAt time.Time `json:"updated_at" db:"updated_at"`
}
