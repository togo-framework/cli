// Code generated by togo. Action — reusable business logic. Call it from a
// controller, or wire it to an event: a.Hooks.On("<event>", 50, action.Run).
package actions

import (
	"context"

	"{{.Module}}/internal/app"
)

// {{.Resource.Name}}Action encapsulates the {{.Resource.Name}} logic.
type {{.Resource.Name}}Action struct {
	App *app.App
}

// New{{.Resource.Name}}Action constructs the action with the app container.
func New{{.Resource.Name}}Action(a *app.App) *{{.Resource.Name}}Action {
	return &{{.Resource.Name}}Action{App: a}
}

// Run executes the action. Implement your logic here. Compatible with the hook
// bus signature, so it can be dispatched on events or queued.
func (x *{{.Resource.Name}}Action) Run(ctx context.Context, payload any) error {
	x.App.Log.Info("action.run", "action", "{{snake .Resource.Name}}")
	// TODO: implement
	return nil
}
