# Code generated by togo. GraphQL fragment — safe to edit. Merged by gqlgen.
type {{.Resource.Name}} {
  id: ID!
{{- range .Resource.Fields}}
  {{camel .Name}}: {{.GQL}}
{{- end}}
  createdAt: Time!
  updatedAt: Time!
}

input Create{{.Resource.Name}}Input {
{{- range .Resource.Fields}}
  {{camel .Name}}: {{.GQL}}
{{- end}}
}

extend type Query {
  {{camel .Resource.Name}}(id: ID!): {{.Resource.Name}}
  {{camel (plural .Resource.Name)}}: [{{.Resource.Name}}!]!
}

extend type Mutation {
  create{{.Resource.Name}}(input: Create{{.Resource.Name}}Input!): {{.Resource.Name}}!
  delete{{.Resource.Name}}(id: ID!): Boolean!
}
