# Code generated by togo. Atlas schema fragment — safe to edit.
table "{{.Resource.Table}}" {
  schema = schema.public
  column "id" {
    type    = uuid
    default = sql("gen_random_uuid()")
  }
{{- range .Resource.Fields}}
  column "{{.Name}}" {
    null = {{.Null}}
    type = {{atlasType .PG}}
  }
{{- end}}
  column "created_at" {
    type    = timestamptz
    default = sql("now()")
  }
  column "updated_at" {
    type    = timestamptz
    default = sql("now()")
  }
  primary_key {
    columns = [column.id]
  }
}
