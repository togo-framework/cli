// Code generated by togo (REST handler). Implement the bodies.
package rest

import (
	"context"
	"net/http"

	"github.com/danielgtaylor/huma/v2"

	"{{.Module}}/internal/models"
)

// {{.Resource.Name}}Output wraps a single {{.Resource.Name}} response.
type {{.Resource.Name}}Output struct {
	Body *models.{{.Resource.Name}} `json:"{{lower .Resource.Name}}"`
}

// Register{{.Resource.Name}}Routes mounts CRUD endpoints for {{.Resource.Table}}.
func Register{{.Resource.Name}}Routes(api huma.API) {
	huma.Register(api, huma.Operation{
		OperationID: "get-{{kebab .Resource.Name}}",
		Method:      http.MethodGet,
		Path:        "/{{.Resource.Table}}/{id}",
		Summary:     "Get a {{.Resource.Name}} by id",
		Tags:        []string{"{{.Resource.Name}}"},
	}, func(ctx context.Context, in *struct {
		ID string `path:"id"`
	}) (*{{.Resource.Name}}Output, error) {
		return nil, huma.Error501NotImplemented("get {{kebab .Resource.Name}} not implemented")
	})
}
