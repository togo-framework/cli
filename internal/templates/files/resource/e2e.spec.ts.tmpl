// Code generated by togo (make:e2e) — Playwright e2e (Dusk-style). Edit freely.
// Run with: npx playwright test  (install once: npm i -D @playwright/test && npx playwright install)
import { test, expect } from "@playwright/test";

test.describe("{{plural .Resource.Name}}", () => {
  test("lists {{plural .Resource.Name}}", async ({ page }) => {
    await page.goto("/{{.Resource.Table}}");
    await expect(page.getByRole("heading", { name: /{{.Resource.Name}}/i })).toBeVisible();
  });

  test("{{.Resource.Table}} API responds", async ({ request }) => {
    const res = await request.get("/api/{{.Resource.Table}}");
    expect(res.ok()).toBeTruthy();
  });
});
