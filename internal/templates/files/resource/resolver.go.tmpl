// Code generated by togo (resolver stub). Implement the bodies.
package resolvers

import (
	"context"
	"fmt"

	"{{.Module}}/internal/models"
)

// {{.Resource.Name}}Resolver groups resolvers for the {{.Resource.Name}} type.
type {{.Resource.Name}}Resolver struct{}

// Get returns a single {{.Resource.Name}} by id.
func (r *{{.Resource.Name}}Resolver) Get(ctx context.Context, id string) (*models.{{.Resource.Name}}, error) {
	return nil, fmt.Errorf("not implemented: {{camel .Resource.Name}} query")
}

// List returns all {{plural .Resource.Name}}.
func (r *{{.Resource.Name}}Resolver) List(ctx context.Context) ([]*models.{{.Resource.Name}}, error) {
	return nil, fmt.Errorf("not implemented: {{camel (plural .Resource.Name)}} query")
}
