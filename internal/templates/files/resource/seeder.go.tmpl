// Code generated by togo (seeder) — edit to insert meaningful sample data.
package seeders

import (
	"context"

	"{{.Module}}/internal/app"
	"{{.Module}}/internal/models"
)

// Seed{{.Resource.Name}} inserts a sample {{.Resource.Name}} via the ORM.
func Seed{{.Resource.Name}}(ctx context.Context, a *app.App) error {
	_, err := models.{{plural .Resource.Name}}(a).Create(ctx, map[string]any{
{{- range .Resource.Fields}}
		"{{.Name}}": {{sample .}},
{{- end}}
	})
	return err
}
