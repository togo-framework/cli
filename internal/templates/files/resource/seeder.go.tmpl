// Code generated by togo (seeder) — edit to insert meaningful sample data.
package seeders

import (
	"context"

	db "{{.Module}}/internal/db/gen"
)

// Seed{{.Resource.Name}} inserts a sample {{.Resource.Name}}.
func Seed{{.Resource.Name}}(ctx context.Context, q *db.Queries) error {
	_, err := q.Create{{.Resource.Name}}(ctx, db.Create{{.Resource.Name}}Params{
{{- range .Resource.Fields}}
		{{pascal .Name}}: {{sample .}},
{{- end}}
	})
	return err
}
