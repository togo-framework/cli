// Code generated by togo (seeder) — uses the factory; adjust the count freely.
package seeders

import (
	"context"

	"{{.Module}}/internal/app"
	"{{.Module}}/internal/factories"
)

// Seed{{.Resource.Name}} inserts fake {{plural .Resource.Name}} via the factory.
func Seed{{.Resource.Name}}(ctx context.Context, a *app.App) error {
	_, err := factories.{{plural .Resource.Name}}(a).Count(5).Create(ctx)
	return err
}
