// Code generated by togo. Next.js page for {{plural .Resource.Name}}.
"use client";

import { use{{pascal (plural .Resource.Name)}} } from "@/lib/hooks/use{{pascal (plural .Resource.Name)}}";

export default function {{pascal (plural .Resource.Name)}}Page() {
  const { data, isLoading } = use{{pascal (plural .Resource.Name)}}();
  if (isLoading) return <p>Loading…</p>;
  return (
    <main className="p-6">
      <h1 className="text-2xl font-bold">{{pascal (plural .Resource.Name)}}</h1>
      <ul>
        {data?.map((item) => (
          <li key={item.id}>{item.id}</li>
        ))}
      </ul>
    </main>
  );
}
